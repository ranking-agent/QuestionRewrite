#!/bin/bash

################################################################
##
## QuestionRewrite accepts a robokop like question and provides similar
## questions to aid the user in discovering related questions that may be more relevant.
## It also provides a web front end and it's own OpenAPI.
##
##   This is the Bash command line interface (CLI) for TranQL.
##
##   author: Phil Owen
##   version: 0.1
##   usage:
##
##     rewrite:
##
##       qrw test [--debug]
##       ex: qrw test
##
################################################################
set -e

##
## Get our location and configure the path.
##
DIR="$( cd "$( dirname "${BASH_SOURCE[0]}" )" >/dev/null 2>&1 && pwd )"
QRW_ROOT=$( dirname $DIR )
export PYTHONPATH=$QRW_ROOT
export QRW=$QRW_ROOT/QRW

##
## Run tests.
##
test () {
    pytest --cov=qrw $QRW/unittests $*
}

$*

exit 0